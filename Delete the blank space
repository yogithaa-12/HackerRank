#include <math.h>
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <assert.h>
#include <limits.h>
#include <stdbool.h>

int main() {
    char s[100];
    fgets(s,sizeof(s),stdin);
    for(int i=0;s[i]!='\0';i++){
        int a=s[i];
        if(a!=32){
            printf("%c",s[i]);
        }
        
    }
    return 0;
}
